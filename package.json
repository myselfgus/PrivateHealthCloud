{
  "name": "private-health-cloud",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Conceptual and contractual scaffold for HealthOS Private Cloud.",
  "scripts": {
    "validate:schemas": "node tools/validate.mjs schemas",
    "validate:examples": "node tools/validate.mjs examples",
    "validate": "npm run validate:schemas && npm run validate:examples"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
